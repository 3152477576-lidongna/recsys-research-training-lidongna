
## 今日作业内容

今天完成第三次课“深度学习环境配置、服务器连接与实验室资源使用”的作业。由于当前无法连接校园网和实验室服务器，本次按照教师说明选择 Mac 本地 CPU 路线，先完成本地 PyTorch 环境的配置与验证。

## 作业完成情况

- [x] 检查本机 Anaconda、Conda 环境和 Python 路径
- [x] 创建独立 Conda 环境 `lesson03`
- [x] 在 `lesson03` 中安装 Python 3.10.18
- [x] 安装 NumPy 1.26.4 和 PyTorch 2.2.2
- [x] 验证 PyTorch 能够正常导入
- [x] 检查 `torch.cuda.is_available()`，结果为 `False`
- [x] 完成张量计算测试，计算结果为 `6.0`
- [x] 安装 `ipykernel`，并注册 `Python 3.10 (lesson03)` Jupyter 内核
（> **环境配置说明**：由于当前无法访问校园网和实验室服务器，本次按照课程要求选择 Mac 本地 CPU 环境路线。课程中的 Miniconda 安装步骤主要用于在实验室服务器个人目录中建立 Conda 环境，而本机此前已经安装 Anaconda，现有 Conda 功能能够正常使用，因此本次未重复安装 Miniconda，而是直接使用 Anaconda 中的 Conda 创建独立的 `lesson03` 环境。该处理不影响 Python 版本隔离、依赖安装和 PyTorch 运行验证。后续具备服务器访问条件后，再按照课程步骤在服务器个人目录中配置 Miniconda 环境。）
## 环境验证结果

- Conda 环境：`lesson03`
- Python 版本：`3.10.18`
- NumPy 版本：`1.26.4`
- PyTorch 版本：`2.2.2`
- CUDA 可用性：`False`
- 张量计算结果：`6.0`
- Jupyter 内核：`Python 3.10 (lesson03)`

`CUDA available: False` 是本地 CPU 环境下的正常结果，不表示 PyTorch 安装失败。

## 遇到的问题与排查过程

PyTorch 安装完成后，首次导入时提示缺少 `numpy` 模块。排查时先确认 `torch==2.2.2` 已正确安装在 `lesson03` 环境中，再检查当前环境的依赖情况。考虑到 PyTorch 2.2.2 与 NumPy 版本的兼容性，使用清华 TUNA 镜像安装 `numpy==1.26.4`。安装完成后重新导入 PyTorch，版本检查、CUDA 可用性检查和张量计算均正常，最终计算结果为 `6.0`。

## 今日收获

通过本次配置，我进一步理解了 Conda 独立环境的作用。不同课程和项目可以使用不同的 Python 与依赖版本，避免影响 `base` 环境和之前的项目。同时明确了本地 CPU 环境与服务器 GPU 环境的区别，理解了 `torch.cuda.is_available()` 返回 `False` 的实际含义。

## 后续计划

具备校园网和实验室服务器访问条件后，再补充 SSH 登录、Remote SSH、服务器 GPU 与 CUDA 检查，并运行 SASRec GPU 快速实验。